<!-- prompt-version: 2 -->

## Section

- Return the outline in document order using levels starting at `1`; never skip a level.
- Each section range must be valid and must cite source block or chunk IDs inside that range.
- A level greater than `1` must follow a prior lower-level parent.
- Use concise source-supported titles; do not create sections from visual style alone.
